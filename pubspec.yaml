name: driver_portal
description: A Driver Portal Application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  permission_handler: ^11.3.0
  flutter_secure_storage: ^9.0.0
  flutter_dotenv: ^5.0.2
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  shared_preferences: ^2.5.3
  http: ^1.2.2
  web_socket_channel: ^2.4.0
  url_launcher: ^6.1.11
  image_picker: ^1.2.0
  table_calendar: ^3.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true